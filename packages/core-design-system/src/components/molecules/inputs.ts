export enum InputState {
  default = 'default',
  focused = 'focused',
  loading = 'loading',
  disabled = 'disabled',
}

export enum InputValidityState {
  valid = 'valid',
  warning = 'warning',
  error = 'error',
}
