export enum FilterStyleProps {
  blur = 'blur',
  brightness = 'brightness',
  contrast = 'contrast',
  grayscale = 'grayscale',
  hueRotate = 'hueRotate',
  saturate = 'saturate',
}
export enum FilterStyleVariant {
  normal = 'normal',
  reversed = 'reversed',
}
