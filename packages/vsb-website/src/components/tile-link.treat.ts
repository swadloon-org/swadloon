import { DesignSystem } from '@newrade/core-design-system';
import { style } from 'treat';

export const wrapper = style((theme: DesignSystem) => ({ width: '100%', height: '720px' }));
export const container = style((theme: DesignSystem) => ({ width: '100%', height: '100%' }));
export const content = style((theme: DesignSystem) => ({ width: '100%', height: '100%' }));
