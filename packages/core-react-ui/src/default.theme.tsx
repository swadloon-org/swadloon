import { createTheme } from 'treat';

export const defautlTheme = createTheme({
  name: 'mir',
  colors: {
    primary: {
      0: 'red',
      100: 'red',
      500: 'red',
      1000: 'red',
    },
  },
});
