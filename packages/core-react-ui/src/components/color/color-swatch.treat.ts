import { styleMap } from 'treat';
import { Theme } from '../../design-system/css-design-system';

//  Todo - transform to style Map

export const styles = styleMap(({ theme, cssTheme }: Theme) => ({
  wrapper: {},
  squareColor: { height: '55px', width: '100%' },
  containerText: {},
  colorName: {},
  shadeNumber: {},
  colorHSL: {},
}));
