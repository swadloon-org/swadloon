describe('the button component (v2)', () => {
  it('should render correctly', () => {
    // TODO jest does not like the .css import
    // const { container } = renderTest(<ButtonV2>Hello world</ButtonV2>);
    // expect(container.firstChild).toMatchSnapshot();
  });
});
