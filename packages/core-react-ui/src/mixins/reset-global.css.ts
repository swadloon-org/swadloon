/* *,
*::before,
*:before,
*::after,
*:after {
  box-sizing: inherit; // border-box everywhere
  -webkit-tap-highlight-color: inherit; // prevent tap highlight
  -webkit-user-drag: none; // prevent element from being dragged on ios
  user-select: none; // no selection by default, we enable it only for specific cases
  outline: none; // no outline by default
  cursor: normal; // normal cursor
} */
