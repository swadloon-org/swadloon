{
  "name": "@newrade/core-react-ui",
  "version": "1.4.1",
  "description": "UI library",
  "license": "MIT",
  "main": "./lib/index.js",
  "module": "./lib/index.js",
  "files": [
    "./lib/**/*"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "rimraf -r dist/** && rm tsconfig.build.tsbuildinfo",
    "start": "tsc --build tsconfig.build.json --watch --preserveWatchOutput",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "typings": "./lib/index.d.ts",
  "dependencies": {
    "@newrade/core-design-system": "^1",
    "@newrade/core-utils": "^1",
    "@newrade/core-jest-config": "^1"
  }
}
