{
  "name": "core-react-ui",
  "version": "1.1.0",
  "description": "UI library",
  "license": "MIT",
  "main": "./lib/index.js",
  "module": "./lib/index.js",
  "files": [
    "./lib/**/*"
  ],
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc --build tsconfig.json",
    "clean": "rimraf -r dist/**",
    "start": "tsc --build tsconfig.json --watch --preserveWatchOutput",
    "test": "echo ok"
  },
  "type": "module",
  "typings": "./lib/index.d.ts",
  "dependencies": {
    "core-design-system": "link:../core-design-system",
    "core-jest-config": "link:../core-jest-config",
    "treat": "^1.5.1"
  }
}
