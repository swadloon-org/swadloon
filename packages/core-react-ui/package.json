{
  "name": "core-react-ui",
  "version": "1.1.0",
  "type": "module",
  "description": "UI library",
  "license": "MIT",
  "main": "./lib/index.js",
  "module": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "files": [
    "./lib/**/*"
  ],
  "scripts": {
    "start": "tsc --build tsconfig.json --watch",
    "build": "tsc --build tsconfig.json",
    "prebuild": "yarn clean",
    "test": "echo ok",
    "clean": "rimraf -r dist/**"
  },
  "dependencies": {
    "core-jest-config": "file:../core-jest-config"
  }
}
