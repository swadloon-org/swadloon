import { Sizing } from 'core-design-system';
import {
  DEFAULT_BASE_FONT_SIZE,
  DEFAULT_SCALING_RATIO,
  DEFAULT_SIZES_CSS_VAR_NAMES,
  DEFAULT_SIZING_STEPS,
  DEFAULT_BASE_FONT_SIZE_PX,
} from 'core-react-ui';

export const sizing: Sizing = {
  baseFontSize: DEFAULT_BASE_FONT_SIZE,
  baseFontSizePx: DEFAULT_BASE_FONT_SIZE_PX,
  ratio: DEFAULT_SCALING_RATIO,
  sizeCSSVarNames: DEFAULT_SIZES_CSS_VAR_NAMES,
  sizingSteps: DEFAULT_SIZING_STEPS,
};
