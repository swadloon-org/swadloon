{
  "name": "@newrade/newrade-website",
  "version": "1.3.2",
  "description": "Gatsby website for newrade's main website",
  "author": {
    "email": "info@newrade.ca",
    "name": "Newrade",
    "url": "https://github.com/newrade/newrade"
  },
  "repository": "https://github.com/newrade/newrade/tree/master/packages/newrade-website",
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn compile",
    "build": "gatsby build",
    "clean": "gatsby clean",
    "compile": "tsc --project tsconfig.gatsby.json",
    "compile:watch": "concurrently \"yarn compile --watch\" \"yarn type-check --watch\"",
    "contentful-types": ". ./.env && contentful-tools generate --config=contentful-tools.js --token=$CONTENTFUL_MANAGEMENT_TOKEN_NEWRADE",
    "pre-debug": "yarn clean && yarn compile",
    "preload-fonts": ". ./.env && PORT=$APP_PORT gatsby-preload-fonts --no-sandbox",
    "refresh": ". ./.env && curl -X POST http://localhost:$APP_PORT/__refresh",
    "serve": ". ./.env && NODE_ENV=production gatsby serve --port=$APP_PORT",
    "prestart": "yarn compile",
    "start": ". ./.env && gatsby develop -p $APP_PORT -H 0.0.0.0",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@newrade/core-design-system": "^1",
    "@newrade/core-react-ui": "^1",
    "gatsby": "^2.24"
  },
  "devDependencies": {
    "@newrade/core-common": "^1",
    "@newrade/core-gatsby-config": "^1",
    "@newrade/core-gatsby-ui": "^1",
    "@newrade/core-utils": "^1",
    "@newrade/core-webpack-config": "^1"
  }
}
