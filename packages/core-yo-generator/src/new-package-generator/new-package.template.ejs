{
  "name": "@<%= scope %>/<%= name %>",
  "version": "1.0.0",
  "type": "module",
  "description": "<%= description %>",
  "scripts": {
    "start": "yarn tsc --watch",
    "build": "yarn tsc",
    "prebuild": "yarn clean",
    "clean": "rimraf -r dist/**"
  },
  "dependencies": {}
}
